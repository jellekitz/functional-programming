import {
  capitalize,
  makeLowerCase,
  noSpace,
  addSpace,
  translateLabel,
} from "./sanitize.js"; // importeer alle schoonmaak functies vanuit sanitize.js

import { displayArticles } from "./render.js"; // importeer de displayArticles function uit render.js

import { dataset } from "./fetch.js"; // importeer de fetch uit fetch.js
